name: Get system file
on:
  workflow_dispatch:
    inputs:
      URL:
        description: "Download URL of the system package to extract files from"
        required: true
      file_name:
        description: "Name of the file to extract"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Maximize build space
        uses: easimon/maximize-build-space@master
        with:
          swap-size-mb: 8192
          root-reserve-mb: 4096
          remove-dotnet: 'true'
          remove-android: 'true'
          remove-haskell: 'true'
      - name: Checkout repository
        uses: actions/checkout@main
      - name: Prepare environment
        run: |
          sudo timedatectl set-timezone Asia/Shanghai
          echo "URL=${{ github.event.inputs.URL }}" >> $GITHUB_ENV
          echo "file_name=${{ github.event.inputs.file_name }}" >> $GITHUB_ENV
          sudo apt install python3 python3-pip aria2 zip p7zip-full tar zstd dos2unix
          sudo apt --fix-broken install
          sudo apt update --fix-missing
          pip3 install --upgrade pip
          pip3 install pycryptodome
          pip3 install setuptools
          pip3 install docopt
          pip3 install requests
          pip3 install beautifulsoup4
          pip3 install --ignore-installed pyyaml
      - name: Extract ROM files
        run: |
          aria2c -x16 -j$(nproc) -U "Mozilla/5.0" -d "$GITHUB_WORKSPACE" -o update.zip "${URL}"
          mkdir -p "$GITHUB_WORKSPACE"/img_port
          for i in system product system_ext mi_ext odm; do
            echo "Extracting $i"
            sudo "$GITHUB_WORKSPACE"/tools/payload_extract -i "$GITHUB_WORKSPACE"/update.zip -t zip --extract=${i} -o "$GITHUB_WORKSPACE"/mods
            info=$(sudo python3 "$GITHUB_WORKSPACE"/tools/gettype.py "$GITHUB_WORKSPACE"/mods/$i.img)
            echo "Decomposing $i ($info)"
            if [ "$info" == "ext" ]; then
              sudo python3 "$GITHUB_WORKSPACE"/tools/imgextractorLinux.py "$GITHUB_WORKSPACE"/mods/$i.img "$GITHUB_WORKSPACE"/img_port >/dev/null
            elif [ "$info" == "erofs" ]; then
              sudo "$GITHUB_WORKSPACE"/tools/extract.erofs -i "$GITHUB_WORKSPACE"/mods/$i.img -o "$GITHUB_WORKSPACE"/img_port -x >/dev/null
            fi
            sudo rm -rf "$GITHUB_WORKSPACE"/mods/$i.img
          done
          mkdir -p "$GITHUB_WORKSPACE"/release
          tmpdir=$(mktemp -d)
          sudo find "$GITHUB_WORKSPACE"/img_port -type f -iname "*${file_name}*" -exec rsync -R {} "$tmpdir" \;
          sudo 7z a -tzip -mx0 "$GITHUB_WORKSPACE"/release/files.zip "$tmpdir"/*
          sudo rm -rf "$tmpdir"
      - name: Post to Release
        uses: softprops/action-gh-release@v1
        with:
          files: ${{ github.workspace }}/release/files.zip
          name: file_${{ github.event.inputs.file_name }}
          tag_name: file_${{ github.event.inputs.file_name }}
          body: |
            - **Extracted file:** ${{ github.event.inputs.file_name }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
